# Story 1.2: PWA Completo — Offline, Install Prompt e Icons

**Epic:** 1 — MVP Core Modules
**Status:** Ready for Review
**Prioridade:** Alta

## Contexto

O build já gera service worker via vite-plugin-pwa, mas falta: icons reais (temos placeholders), install prompt, e garantir que os módulos Canvas/Projetor funcionam offline.

## Acceptance Criteria

- [x] Icons SVG/PNG profissionais (192px e 512px) com identidade visual do app
- [x] Splash screen iOS (apple-touch-icon + meta tags)
- [x] Install prompt customizado (banner "Adicionar à tela inicial")
- [x] Precache de todos os SVGs da biblioteca para uso offline
- [x] Módulos Biblioteca, Estúdio e Simulador funcionam 100% offline
- [x] Projetor mostra mensagem adequada se offline (câmera precisa de rede? não)
- [x] manifest.json com screenshots para install dialog rico

## Technical Notes

- vite-plugin-pwa já configurado em `vite.config.js`
- Adicionar `workbox` runtime caching para SVGs em `public/biblioteca_riscos/`
- `beforeinstallprompt` event handler para install banner

## File List

- [x] `public/icon-192.png` — botanical icon (192x192)
- [x] `public/icon-512.png` — botanical icon (512x512)
- [x] `public/apple-touch-icon.png` — iOS icon (180x180)
- [x] `vite.config.js` — workbox caching (SVGs CacheFirst + Google Fonts)
- [x] `src/components/InstallPrompt.jsx` — novo (beforeinstallprompt handler)
- [x] `src/App.jsx` — InstallPrompt integrado
- [x] `index.html` — apple-mobile-web-app meta tags + apple-touch-icon
